# Core extraction and cleanup
pymupdf>=1.26
ebooklib>=0.20
beautifulsoup4>=4.12
lxml>=5.0
Pillow>=10.0

# Text-to-speech (piper is the default engine, edge-tts is an alternative)
piper-tts
edge-tts

# Scanned-PDF OCR. Also requires the Tesseract program installed on your PATH.
pytesseract>=0.3.13
